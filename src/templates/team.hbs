{{!-- our tem --}}

<section class="films">
  <div class="container">
    <ul class="team__list">
      {{#each this}}

      <div class="team__container">
        <li class="team__item">
          <img class="team__img" src="{{img}}" alt="human">
          <p class="team__name">{{name}}</p>
        </li>
      </div>
      {{/each}}
    </ul>
  </div>
</section>